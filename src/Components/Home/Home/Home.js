import React from 'react';
import BookingForm from '../BookingForm/BookingForm';

const Home = () => {
    return (
        <div>
           <BookingForm></BookingForm> 
        </div>
    );
};

export default Home;